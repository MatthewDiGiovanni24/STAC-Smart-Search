# Copy to .env for local (non-Docker) runs. Docker Compose sets these directly.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/stac
CMR_STAC_ROOT=https://cmr.earthdata.nasa.gov/stac/
DISCOVERY_TIMEOUT_SECONDS=30
LOG_LEVEL=INFO
